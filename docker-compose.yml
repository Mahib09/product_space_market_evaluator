services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - cache_data:/app/cache.db
      - eval_data:/app/evaluations.db

volumes:
  cache_data:
  eval_data:
